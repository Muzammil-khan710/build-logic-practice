// Write a program that reads two strings and append first string to the second

let firstString = prompt("enter first string")

let secondString = prompt("enter second string")

const result = secondString + firstString;

console.log(result)

// we can also use concat method

// const result = secondString.concat(firstString)
